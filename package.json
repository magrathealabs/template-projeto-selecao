{
  "name": "birthdays",
  "version": "1.0.0",
  "scripts": {
    "setup": "npm install && cd client && npm install",
    "client": "cd client && npm start",
    "server": "cd api && node server.js",
    "dev": "concurrently --kill-others-on-fail \"cd api && nodemon server.js\" \"npm run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.1",
    "nodemon": "^1.19.1"
  }
}
